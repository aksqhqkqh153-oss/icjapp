# First Push and Deploy Quickstart

1. Copy this project into C:\Users\icj24\Downloads\historyprofile_app\n2. Run git init, set main, add origin, commit, push.
3. Connect GitHub repo to Cloudflare Pages and Railway.
4. Cloudflare Pages root=frontend build=npm run build output=dist
5. Railway uses Dockerfile / backend FastAPI service.
